[build]
  base    = "client"
  publish = "dist"
  command = "npm run build"

[build.environment]
  VITE_DEMO_MODE = "true"

# SPA fallback — send all routes to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
